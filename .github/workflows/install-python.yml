name: Install Python

on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Which
        run: which python

      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.7

      - name: which python
        run: which python
