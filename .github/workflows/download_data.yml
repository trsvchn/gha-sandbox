name: Download data

on: [push]

jobs:
  job1:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Ls
        run: ls

      - name: Get data
        uses: trsvchn/storage-action@main

      - name: Ls
        run: ls

      - name: REad file
        run: cat copy_this.file
